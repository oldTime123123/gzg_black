# Theme2 New Theme Master Plan

## 结论

`theme2` 不适合继续沿用 `theme1` 的薄荷绿方向做小修小补。  
这次更适合直接重做成一套 **新的第二主题**：

**Titanium Amber / 钛金琥珀金融终端主题**

核心方向：

- 深钛灰底
- 冷灰层次
- 低饱和琥珀品牌强调
- 红涨绿跌保持交易语义
- 更克制的背景图
- 更稳的 H5 交易页面结构

这份文档是 `theme2` 后续重构的主思路文档，重点用于指导：

- 新主题方向
- 背景图制作
- 元素布局
- 弹窗
- 输入框
- 空状态 / loading
- 移动端适配

---

## 1. 项目理解

### 1.1 当前项目结构

`theme2` 仍然是一套完整金融 H5：

- `pages/tabbar/home.vue`
- `pages/tabbar/market.vue`
- `pages/tabbar/invest.vue`
- `pages/tabbar/news.vue`
- `pages/tabbar/mine.vue`
- `pages/trade/*`
- `pages/pay/*`
- `pages/setting/*`
- `pages/fund/*`
- `pages/loan/*`
- `pages/record/*`
- `pages/auth/*`

说明它不是 demo，而是一整条交易、资金、账户链路。

### 1.2 当前主题状态

从 [public.less](/Users/apple/Desktop/word/componey/stock/GZG_THEME/theme2/assets/css/public.less) 看，当前 `theme2` 已经不是旧蓝白，而是：

- 深色底
- 薄荷绿主色
- 红涨绿跌语义

但当前仍然存在几个问题：

1. 主题方向和 `theme1` 过近，区分度不够
2. 资产目录里保留了大量旧背景图、旧图标、旧装饰资源
3. 页面结构虽然有优化，但还没有彻底形成新的主题系统
4. 弹窗、输入框、二级页骨架、空状态和 loading 还需要继续统一

### 1.3 为什么 `theme2` 要做第二主题

`theme2` 最适合承担“第二主题”角色，原因有三点：

1. 它页面链路完整，适合做成一套真正可交付的新主题
2. 当前薄荷绿路线已经比较成熟，再做一版变体价值不高
3. 它仍然保留很多旧图片和旧结构，适合借这次机会彻底统一

---

## 2. 新主题方向

### 2.1 推荐方向

建议 `theme2` 后续统一采用：

**Titanium Amber / 钛金琥珀**

关键词：

- 深钛灰
- 金属灰
- 低饱和琥珀
- 更成熟的金融终端感
- 稳定、可信、克制

### 2.2 视觉目标

和当前主题相比，新主题应该达到：

- 首屏更稳
- 品牌辨识度更强
- 不再依赖大量旧图片背景
- 弹窗和表单语言更统一
- 更适合高密度交易信息

### 2.3 语义规则

交易语义必须固定：

- `is_rise == 2` => 涨 => 红色
- `is_rise == 1` => 跌 => 绿色

品牌琥珀不能代替涨跌色。

---

## 3. 色彩与 Token 方向

建议 `theme2` 后续收敛为：

```css
:root {
  --brand-primary: #d49a3a;
  --brand-primary-hover: #e1ad58;
  --brand-primary-active: #bf8426;
  --brand-primary-soft: rgba(212, 154, 58, 0.14);
  --brand-primary-border: rgba(212, 154, 58, 0.26);

  --surface-page: #0c1016;
  --surface-panel: #121922;
  --surface-card: #18212c;
  --surface-card-soft: #1d2733;
  --surface-card-strong: #1f2b38;
  --surface-elevated: #273443;

  --text-primary: #f4f6f9;
  --text-secondary: #b9c2ce;
  --text-muted: #7d8999;
  --text-placeholder: #667281;

  --color-up: #f04452;
  --color-down: #18c37e;
  --color-warning: #f4b740;
  --color-info: #5daeff;

  --border-soft: rgba(255, 255, 255, 0.08);
  --border-default: rgba(255, 255, 255, 0.12);
  --border-strong: rgba(212, 154, 58, 0.2);
}
```

原则：

- 不大面积使用纯黑
- 不再保留主题感过强的薄荷绿主视觉
- 琥珀只负责品牌与导航
- 卡片通过 `surface` 分层，不靠重边框

---

## 4. 背景图制作思路

### 4.1 背景图职责

背景图只负责气氛，不负责承载信息。

它可以承担：

- 品牌氛围
- 金属反射
- 弱数据线
- 低透明光晕

不能承担：

- 标题
- 关键数字
- 表单内容
- 按钮文案

### 4.2 推荐背景制作方向

推荐统一成：

- 深色渐变底
- 弱金属光泽
- 局部琥珀高光
- 极轻网格或金融线条
- 更克制的噪点层

### 4.3 页面级背景策略

#### 认证页

- 背景可相对更有品牌感
- 但不能压住输入框
- 更偏“登录中枢”而不是活动页

#### 首页 / 我的页

- 可以保留一定品牌氛围
- 但正文区必须干净

#### 交易 / 记录 / 资金页

- 背景应更克制
- 重点让卡片和数字稳定可读

### 4.4 后续应替换的旧资源类型

优先替换：

- `assets/images/img/*Bg.png`
- 旧业务装饰图
- 旧 tabbar 图标
- 旧功能 PNG 图标
- 旧空状态图

---

## 5. 页面布局思路

### 5.1 首页

首页建议做成：

- 首屏主资产 / 主行情看板
- 交易入口 rail
- 热门股票简洁卡
- 资讯头条 + 简讯

避免继续：

- 多个等权模块堆叠
- 旧式活动页结构
- 资讯和行情同权混排

### 5.2 市场页

市场页建议强调：

- 搜索
- 指数主卡
- 自选 / 市场切换
- 更像看板，不像堆列表

### 5.3 持仓 / 记录页

建议做成：

- 顶部资产概览
- 中部状态切换
- 下部记录流

重点：

- 卡头结构统一
- 长标题、长数字、长时间优先完整显示
- 价格、箭头、涨跌额、涨跌幅统一语义

### 5.4 交易页

交易页建议固定成：

1. 报价区
2. 输入区
3. 提示区
4. 确认区

移动端优先单列，不要压缩复杂输入。

### 5.5 用户中心

应固定成：

- 主账户卡
- 资金矩阵
- 快捷动作
- 功能入口

同一身份文案、会员信息、说明文案只保留一处。

### 5.6 二级页骨架

后续所有二级页尽量统一成：

1. `SecondPageNavBar`
2. Hero / 说明卡
3. 主内容卡
4. 底部主操作区

禁止继续出现：

- 导航标题和 Hero 标题重复
- Hero 里堆两层同义说明
- 卡片标题继续重复页面名

---

## 6. 元素布局规范

### 6.1 卡片

卡片应明确分成：

- 大卡：主信息承载
- 小卡：状态 / 数据摘要
- 列表卡：记录流

规则：

- 内边距统一
- 卡片间距统一
- 标题、数值、说明层级明确

### 6.2 Badge / Chip / Pill

所有有背景色的短标签都必须提前考虑：

- 多语言变长
- 小屏空间变窄
- 非独占一行时的溢出

原则：

- 能重排就重排
- 能换行就换行
- 不随便 `ellipsis`

### 6.3 长数字

价格、金额、卡号、时间、订单号都要提前防炸。

建议方式：

- 减小字号
- 拆层展示
- 重排卡头
- 拆成主价格 / 涨跌额 / 涨跌幅

不要直接把关键数字截断。

---

## 7. 弹窗思路

### 7.1 弹窗必须纳入主题系统

弹窗不是局部补丁，必须统一成同一套主题语言。

统一要素：

- 弹窗背景
- 标题层级
- 关闭按钮
- 统计区
- 表单区
- 按钮区

### 7.2 推荐弹窗结构

统一使用：

1. `dialogHead`
2. `dialogTitle`
3. `dialogCode / subtitle`
4. `dialogSummary`
5. `fieldGroup`
6. `dialogFooter`

### 7.3 后续要重点统一的弹窗

- IPO / SPO / 交易确认弹窗
- 实名提示弹窗
- 银行选择弹窗
- 删除确认弹窗
- 订单处理弹窗

### 7.4 禁止出现的弹窗问题

- 默认白底弹窗直接裸用
- 同类场景弹窗结构不一致
- 弹窗里再套多余小盒子
- 标题 / 代码 / 统计项堆得太近

---

## 8. 输入框与表单思路

### 8.1 输入框统一规则

建议统一：

- 高度 `52px ~ 56px`
- 深色底
- 大圆角
- 轻描边
- 清晰 focus 态

### 8.2 输入布局规则

优先：

- 标签在上
- 输入框在下
- 说明文案放输入框外
- 复杂表单优先单列

### 8.3 图标规则

- 左 icon 固定宽度
- 右 icon 放在固定容器
- 显隐 / 选择 / 清除 icon 风格统一

### 8.4 选择类组件

底部选择器应满足：

- 文本居中
- 当前选中态明显
- 列表有最大高度
- 超出后可滚动
- iOS 滚动平滑

---

## 9. 空状态与 Loading

### 9.1 空状态

`theme2` 后续不建议继续保留旧图片式空状态。

建议统一成：

- 深色容器
- 抽象图形
- 简短标题
- 简短说明

### 9.2 Loading

Loading 要统一成当前主题语言的一部分：

- 深色图表骨架
- 深色卡片 skeleton
- 弱金色 / 冷灰 shimmer

不再保留白底 loading 和风格断裂的过时 loading。

---

## 10. 移动端适配要求

### 10.1 必查宽度

必须重点检查：

- `320px`
- `375px`
- `390px`
- `430px`

### 10.2 必查问题

1. 长标题不溢出
2. 长金额不溢出
3. badge 不撑爆
4. sticky nav 有背景
5. fixed button 不压内容
6. picker / popup 内部可滚动
7. 返回顶部不和 Tabbar 重叠
8. safe-area 适配正常

### 10.3 适配原则

- 优先改单列
- 优先重排
- 优先完整显示关键数字
- 不靠无脑缩字和截断

---

## 11. 后续执行顺序

### 阶段 1：系统层

优先统一：

- Token
- `Tabbar`
- `TabbarTopNavBar`
- `SecondPageNavBar`
- Button
- Input
- Dialog / Popup
- Empty / Loading

### 阶段 2：五个核心 tab 页

1. `tabbar/home.vue`
2. `tabbar/market.vue`
3. `tabbar/invest.vue`
4. `tabbar/news.vue`
5. `tabbar/mine.vue`

### 阶段 3：交易与资金链路

- `trade/*`
- `pay/*`
- `setting/*`

### 阶段 4：资产与记录链路

- `fund/*`
- `loan/*`
- `record/*`

### 阶段 5：统一收口

- 长文本与长数字
- badge / chip / pill
- picker / popup
- 多语言
- 空状态
- loading
- 背景图残留

---

## 12. 验收标准

### 12.1 视觉

- `theme2` 和 `theme1` 一眼可区分
- 主题语言统一
- 琥珀只做品牌强调
- 红涨绿跌语义稳定

### 12.2 结构

- 页面标题不重复
- 首页、交易、持仓、我的页首屏主模块明确
- 二级页骨架统一

### 12.3 交互

- 弹窗可正常开关
- 输入区可正常提交
- picker 有清晰选中态
- fixed 操作区不遮挡内容

### 12.4 移动端

- 小屏不破版
- 长文案不炸
- 长数字不炸
- safe-area 正常
- sticky 区不遮内容

---

## 13. 不允许继续出现的情况

- 继续沿用大量旧 PNG 背景直接拼页面
- 页面标题和 Hero 标题重复
- 默认白底弹窗直接裸用
- 同类弹窗结构多套并存
- 输入框、按钮、标签尺寸不统一
- 重要数字直接被省略掉关键内容
- badge / chip 有背景色但不做溢出保护
- H5 页面继续照搬桌面式双栏
- 空状态 / loading 风格断裂

---

## 14. 最终建议

`theme2` 这次不要继续做“薄荷绿增强版”，而应该明确做成：

**一套真正区别于 `theme1` 的第二主题。**

如果后续执行，这套新主题最适合的题目就是：

> 为 `theme2` 重做一套 Titanium Amber 钛金琥珀金融终端主题，覆盖首页、交易、订单、用户中心及全部二级页，统一背景图、弹窗、输入框、空状态、loading 和移动端适配规则，并保持交易语义红涨绿跌不变。
